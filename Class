/* Ստեղծել Course class՝
Name
MaxStudents
EnrollStudent(Student student)
Ստեղծել Student class՝
Name
Age
Պահանջներ
Ստեղծել 2 student
Գրանցել course-ում
Տպել արդյունքը 

using System;
using System.Collections.Generic;

namespace lab2
{
    public class Course
    {
        public string Name { get; private set; }
        public int MaxStudents { get; private set; }

        private List<Student> students = new List<Student>();

        public Course(string name, int maxStudents)
        {
            Name = name;
            MaxStudents = maxStudents;
        }

        public void EnrollStudent(Student student)
        {
            if (students.Count < MaxStudents)
            {
                students.Add(student);
            }
            else
            {
                Console.WriteLine("You exceeded MaxStudents!");
            }
        }

        public void PrintStudents()
        {
            Console.WriteLine("Course: " + Name);

            foreach (Student student in students)
            {
                Console.WriteLine($"{student.Name} : {student.Age}");
            }
        }
    }

    public class Student
    {
        public int Age { get; private set; }
        public string Name { get; private set; }

        public Student(int age, string name)
        {
            Age = age;
            Name = name;
        }
    }

    internal class Program
    {
        static void Main()
        {
            Student student1 = new Student(19, "Yelena");
            Student student2 = new Student(20, "Nane");

            Course course1 = new Course("Programming", 54);

            course1.EnrollStudent(student1);
            course1.EnrollStudent(student2);

            course1.PrintStudents();
        }
    }
}
*/



/*1. Գրիր class Student, որը․
○ Ունի Name, Age, StudentId,
○ Static field → Count (քանի ուսանող է ստեղծվել),
○ Constructor chaining տարբերակներով,
○ Console demo՝ ստեղծիր 3 ուսանող։

using System;
using System.Threading;


namespace ConsoleApp1
{
    internal class Program
    {
        static void Main()
        {
            Student first= new Student();
            first.Print();
            Student second= new Student(1,"Lilit",18);
            second.Print();
            Student third = new Student("Nane");
            third.Print();

            Console.WriteLine("The current number of students: {0}",Student.Count);

        }
    }

    public class Student
    {
        public int StudentId { get;private set; }
        public string Name { get;private set; }
        public int Age { get;private set; }
        public static int Count=0;

        public Student() : this(0, "Unknown", 18) { }
        public Student(string name) : this(0, name, 18) { }
        public Student(int studentId, string name, int age)
        {
            StudentId = studentId;
            Name = name;
            Age = age;
            Count++;
        }

        public void Print()
        {
            Console.WriteLine($"{StudentId}. {Name}: {Age}.");
        }
    }

}
*/



